---
description: Implement a backlog item or feature end to end as a vertical slice
---

Implement the following as a complete vertical slice: $ARGUMENTS

Follow the `vertical-slice` skill exactly. Before writing code, read the architecture docs the
feature touches and state which layer each new file belongs to and why (teaching mode — `CLAUDE.md`).

Do not commit. When finished, report: files added or changed, the layering reasoning, the tests
written, and anything you deliberately left out.
