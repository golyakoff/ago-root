---
description: Write a new ADR for a decision just made
---

Write an ADR for: $ARGUMENTS

Follow the `adr-writer` skill. First check whether this actually warrants an ADR; if it does not,
say so and name the convention doc that already covers it.

Use the next free number, `docs/adr/_template.md`, and add the row to `docs/adr/README.md` in the
same change. The Consequences section must contain real negatives, and Alternatives must name the
option most teams would pick in production and why we did not.
