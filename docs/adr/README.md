# Architecture Decision Records

One file per decision a reviewer could reasonably argue with. Numbered, and immutable once accepted:
a decision that changes gets a **new** ADR superseding the old one, and the old one stays with its
status updated. Rewriting history here destroys the only artefact showing how the design was reasoned about.

**These files say _why_. They are not where you look up how the system behaves now** (`adr/0156`).
That question is answered by `docs/architecture/*` and `docs/conventions/*`, which are authoritative
for current state - one document on a topic instead of reconstructing a chain across 129 files, and
correct by construction rather than by careful reading.

**"Immutable" is now enforced rather than stated.** The only edit an accepted ADR may receive is its
`Status` line gaining a supersession or amendment pointer. **Appending an amendment to the body is
forbidden** - it is what made this corpus unreadable, because a file can then look current and carry
its own reversal several screens down, and no index built over it can be trusted. An amendment is a
new ADR.

**Every ADR is referenced from at least one current-state document, in the change that accepts it.**
`tools/queue-audit.sh` names any that is not. That check is not tidiness: without it, a document that
silently omits a decision gives a confident wrong answer, which is worse than the slow reconstruction
this split replaced.

Write an ADR when a technology is chosen over an alternative, a guarantee is weakened or strengthened,
a boundary is crossed deliberately, or a reviewer would ask "why on earth". Do not write one for
naming, formatting, or anything a convention doc already covers.

| # | Decision | Status |
|---|---|---|
| 0001 | Record architecture decisions | Accepted |
| 0002 | Clean Architecture layering and the dependency rule | Accepted |
| 0003 | Platform/product split, modular monolith, two hosts | Accepted — host count amended by 0013 |
| 0004 | PostgreSQL, EF Core for writes, Dapper for reads | Accepted |
| 0005 | Transactional outbox for reliable publishing | Accepted |
| 0006 | Broker abstraction at topic + partition key + at-least-once | Accepted |
| 0007 | Connection registry instead of a SignalR backplane | Accepted |
| 0008 | Presigned direct-to-storage uploads | Accepted |
| 0009 | Redis is a cache and coordination store, never truth | Accepted |
| 0010 | No sticky sessions at the edge | Accepted |
| 0011 | All instants are UTC `DateTimeOffset`, rendered per request | Accepted |
| 0012 | Multiple repositories, platform published as packages | Accepted |
| 0013 | Three deployables split by failure profile; webhooks as a bulkhead | Accepted |
| 0014 | NGINX Gateway Fabric (Gateway API) instead of ingress-nginx | Accepted |
| 0015 | `ago-chat`'s CI packs `ago-platform` from source, no hosted feed yet | Accepted |
| 0016 | Granular permissions (RBAC), scoped per tenant, as the authorization model | Accepted |
| 0017 | Outbox/inbox writer is generic over `DbContext`, not per-product | Accepted |
| 0018 | `ago-platform` publishes to GitHub Packages; `ago-chat`'s CI restores from it | Accepted |
| 0019 | Partitioning `messages` widens its unique index to include `created_at` | Accepted |
| 0020 | Node-delivery fan-out publishes directly, bypassing the outbox | Accepted |
| 0021 | Operator assignment: `SKIP LOCKED` (default) vs. a per-operator Redis lock | Accepted |
| 0022 | OIDC via Keycloak replaces the operator dev-auth stub | Accepted |
| 0023 | React for `ago-console` | Accepted |
| 0024 | Webhook HMAC-SHA256 signing; secret stored as AES-256-GCM ciphertext, not hashed | Accepted |
| 0025 | Direct OTLP export to Jaeger, no collector | Accepted |
| 0026 | k3s VPS hosting, `*.reserve-me.ru` domain plan, VM sizing, and TLS | Accepted |
| 0027 | Operator identity across products: separate entities, unified only through Keycloak | Accepted |
| 0028 | Keycloak-native self-registration; `RequireKeycloakIdentity` stays distinct from `RequireOperatorIdentity` | Accepted |
| 0029 | Widget config is fixed fields, read at bootstrap | Accepted |
| 0030 | Design tokens and a closed hand-rolled component set for the console, not a component library | Accepted |
| 0031 | History retention: an immutable retention class in the partition key, an archive instead of deletion | Accepted |
| 0032 | The platform owner is a Keycloak realm role, deliberately outside the per-site RBAC model | Accepted |
| 0033 | A capacity claim is a receipt on the conversation, not an assumption about assignment | Accepted — **partly superseded by 0105**: its Consequences said manual assignment stays capacity-blind, and 23-04 made a deliberate take charge capacity uncompared. The Decision itself — the receipt, release after commit, xmin-arbitrated idempotency — stands unchanged |
| 0034 | The realm's login-security policy, and every token lifetime, chosen rather than inherited | Accepted |
| 0036 | Keycloak on a separate database inside the shared Postgres; `start-dev` stays; realm settings stop arriving by restart | Accepted - a second logical database with its own role, not a second Postgres instance and not a schema inside `ago_chat`: an instance would spend 256 MiB of requests permanently out of the headroom `adr/0026` already accepts an OOM risk within, to isolate a workload measured in kilobytes; a shared schema would put Liquibase and EF Core migrations on the same tables. States the cost rather than hiding it - **an outage of the application's database is now also an authentication outage** - and argues that on a one-node cluster the correlation is close to free, because an IdP that survives while nothing can be authenticated *for* is not a better outcome. That reasoning is a property of this deployment, not a general rule |
| 0037 | The capacity release absorbs the assignment engine's deadlock | Accepted - `ReleaseAsync` retries `40P01` up to five times with jittered backoff, and **only when it owns no caller transaction**: inside one the deadlock has already aborted the transaction and the next statement could only fail `25P02`, so there the retry unit is the caller's own transaction and its caller is a redelivering consumer. A surviving `40P01` becomes `OperatorCapacityContentionException` at the port boundary, the same translation `6-08` gave `ConversationConcurrencyConflictException`, so Application never names Npgsql and an operator never sees a Postgres code. `CloseConversationHandler` keeps the close **successful** - the save committed before the release was attempted - and counts the abandoned release rather than failing a close that already happened |
| 0040 | Keycloak's SMTP is a realm setting supplied as a Secret; a sink locally; the sending provider is the author's call | Accepted, provider sub-decision open |
| 0044 | Delivery is dimensioned by recipient kind and presence; the platform reports facts, the product names them | Accepted |
| 0045 | Alerting reaches a person by email through the node's own Postfix; Prometheus rules and Alertmanager, not Grafana | Accepted |
| 0046 | `Ago.Platform.Hosting` carries only the module seam; telemetry ships as `Ago.Platform.Observability` | Accepted |
| 0047 | Images go to GHCR under a commit-SHA tag, and a rollback is a first-class operation | Accepted |
| 0048 | Visitor sessions renew at the point of use; the token drops to seven days, sliding, with no absolute cap | Accepted |
| 0049 | AGO Calendar's time model, and no-overlap as a database exclusion constraint | Accepted |
| 0050 | Backup scope per store, the author's own machine as the destination, and a key the node does not hold | Accepted |
| 0051 | A frontend image takes no environment input from its build command; the commit determines the artifact | Accepted |
| 0052 | *(never written - the number was reserved for `17-08` and the item turned out to need no decision of its own; kept vacant rather than reused, so a reference to `0052` anywhere is a mistake rather than a different ADR)* | n/a |
| 0053 | Availability materialisation is day-granular and insert-only | Accepted |
| 0054 | Runtime hardening: what each workload is constrained to, and what is deliberately not | Accepted |
| 0055 | External channel identity, and the shape of the inbound channel port | Accepted |
| 0056 | Schema migrations are a separate deployable that runs before the hosts | Accepted |
| 0057 | How long logs, traces and metrics are kept, and what actually enforces it | Accepted |
| 0058 | Demo credentials are minted per viewer, with a tenant of their own | Accepted |
| 0059 | The booking claim is a compare-and-set, and a lost race is not an error | Accepted |
| 0061 | A message can carry structure AGO Chat does not understand | Accepted |
| 0062 | *(never written - reserved for `5-19`, which turned out to need no decision of its own: `adr/0048` already carried the precedent. Kept vacant rather than reused)* | n/a |
| 0063 | A principal's kind is answered per surface, by asking the positive question | Accepted |
| 0064 | AGO Calendar's console is its own repository; its booking UI is not | Accepted |
| 0065 | AGO Chat carries a module's steps without understanding them | Accepted |
| 0066 | An auto-reply fires only when nobody is on duty, and is authored by the system | Accepted |
| 0067 | One key issues, several validate, and retirement is a configured delay | Accepted |
| 0068 | One identity may hold several tenancies, resolved per request, never baked into the token | Accepted |
| 0069 | Channel credentials are a shared shape, with two different secrets held two different ways | Accepted |
| 0070 | Telegram outbound calls routed through a VLESS relay | Accepted |
| 0071 | ЮKassa webhook signature scheme and credential shape | Accepted |
| 0072 | Tenant export format, versioning, and why attachment bytes are referenced, not embedded | Accepted |
| 0073 | Subscription lifecycle: recurring charge, retry, cancellation, mid-cycle change, seat assignment | Accepted |
| 0074 | Retention archive: precise attachment expiry, and retrieval as a direct read | Accepted |
| 0075 | An explicit-transaction port, and a canonical lock order, for transferring a conversation | Accepted |
| 0076 | AGO is controller for its own accounts, processor for a tenant's visitor data - and the widget carries the tenant's notice | Accepted |
| 0077 | A chat module's transport is the wire, confirmed by a real measured hop, not a package reference | Accepted |
| 0078 | AI automation is several different capabilities, not one feature, and most of them fit the existing module contract unchanged | Accepted |
| 0079 | Verified channel-identity linking, unlinking, and a preferred reply channel | Accepted |
| 0080 | The email channel has no per-tenant credential, and threads by subaddress | Accepted |
| 0081 | A reachable-but-unsure module escalates through a fifth primitive, not a new signal | Accepted |
| 0082 | Phone verification gets a fourth rate-limit bucket, keyed on the caller's own IP | Accepted |
| 0083 | Calendar's account owner is an aggregate invariant, not a bypass | Accepted |
| 0084 | A worker's schedule is a template of two kinds, and a cycle chooses days, not hours | Accepted |
| 0085 | Re-cutting an already-materialised horizon is a second, manual, destructive entry point | Accepted |
| 0086 | The booking claim generalises from one row to a claimed set | Accepted |
| 0087 | `messages` is partitioned by tenant hash, not by time | Accepted — amends 0031's mechanism and 0019's index shape |
| 0088 | A chat operator acts on a booking as a real Calendar `Operator`, invited by email | Accepted — applies 0027 a second time, amends nothing |
| 0089 | A channel poller is owned by one process, per credential, by a Postgres advisory lock | Accepted — amends `concurrency.md`'s unqualified multi-replica claim; declines a platform promotion |
| 0090 | An operator creates bookings directly, and a reschedule is a chain rather than an edit | Accepted — extends 0086 without replacing its anchor identity; scopes `20-09`'s verified-phone rule to two of three origins |
| 0091 | The bare product name belongs to the console, the API takes an `-api` suffix | Accepted — amends 0026's subdomain map; the widget's build-time origin (0051) is why the chat half is staged in three steps |
| 0092 | The widget script is served from the API origin, under `/widget/` | Accepted — decides `#324`; chooses the API's own host over a `widget.`/`cdn.` one specifically to make origin-inference possible and stop paying 0051's build-time bake |
| 0093 | Tenancy and identity unify across products; domains stay apart | Accepted — supersedes 0027's tenancy/identity half (its domain split stands) and 0091's per-product-console premise; rejects a platform-hosted `Tenant` on the ground that `Ago.Platform.*` ships as packages (0012), so it would be a shared type with no shared data |
| 0094 | A module call carries a short-lived assertion chat signs, not a bearer secret | Accepted — extends 0065's module contract with a `Credential` beside `EntryPoint`; names the per-deployment (not per-site) secret as the limit `22-04` must close, and records mTLS as the production answer deliberately not taken |
| 0095 | A module registration is provisioned over a generic contract, bootstrapped by a deployment-wide provisioning secret | Accepted — extends 0065's contract with a second route family and 0094's credential with a way to install it; records a blast radius **worse** than 0094's own stated limit, accepted because a bootstrap anchor cannot be scoped to the row it creates, and points at `22-18` as the item that removes the exposure rather than mitigating it |
| 0097 | A `Competing` subscription's queue lifetime is explicit, never inferred, never swept | Accepted — amends `messaging.md`'s unconditional `Competing` durability with one named carve-out; rejects a janitor and name-inference, and records that tying the dead-letter queue's lifetime in broke a pre-existing test against a real broker |
| 0098 | An owner's grant is recorded on the entitlement itself, and expires only on the granting side | Accepted — a second **caller** of `22-11`'s registration mechanism, not a second mechanism; puts `GrantedByOwner`/`ExpiresAt` on the `EnabledModule` row rather than an audit table that could disagree with it, makes `ExpiresAt` `required`-but-nullable so a perpetual grant must be stated, and records that expiry binds chat only — the module is never told a grant lapsed |
| 0099 | A module call's refusal reason lives in the log, not on the wire | Accepted — extends 0094/0095 with a six-reason classification; keeps the wire flat on a disclosure argument, rejects the almost-authenticated half-measure because timing leaks the same signal, and records `22-18` as the condition under which the decision should be revisited |
| 0100 | The caller names the calendar tenant, and the read that authorizes is the read that verifies it | Accepted — applies `adr/0068`'s `X-Ago-Active-Site` to `ago-calendar` rather than inventing a second name for the same id (`adr/0093` makes a `TenantId` a `SiteId`); puts the verification *inside* the authorizing query so a caller-named tenant can only narrow, never widen; refuses a tenant the caller holds nothing in rather than falling back to one they do, diverging deliberately from `ago-chat`'s "unrecognised means not asked"; rejects a path segment (blast radius, and it degrades the single-tenant case), a re-issued sign-in claim (`adr/0022` mints no token) and a Redis session tenant (rule 8); records that an ordinary operator now joins the platform owner in the caller-chosen-tenant category |
| 0101 | An assignment interval carries no foreign key to its conversation | Accepted — `conversation_assignments` (`23-03`) drops the default cascading key on `conversation_id`, because `16-02`'s erasure deletes the whole `conversations` row and a cascade would silently take a tenant's workload history with it, reversing `decisions.md` §2's *erasing a conversation need not take last month's numbers with it*; `site_id`/`operator_id` keep ordinary cascades; rejects cascade-by-default (silent, undetected loss), `ON DELETE SET NULL` (no honest null for a conversation that existed), and the explicit-drain shape `conversation_notes`/`conversation_tags` use (this table holds no personal data to remove); guarded by `ConversationAssignmentErasureGuardTests`, which passes today by construction and exists so a key added later for symmetry cannot reverse this with nothing going red |
| 0103 | A rate carries its own fraction, a figure carries its own preceding period, and a thin sample never ranks | Accepted — applies `decisions.md` §7's 2026-09-04 amendment, which reversed its own decision from *refuse to print a thin rate* to *print it with the figures it came from*; computes the preceding-period comparison by calling each existing single-window read-store port twice from its handler rather than adding a second port or new SQL, on the argument that which two windows to compare is Application policy and the port stays a projection; consumes `AnalyticsOptions.MinimumSampleForRate` only inside `ConversionReportReadStore`/`TagBreakdownReadStore`, the two lists that actually rank on a rate, and records why `OperatorAnalyticsReadStore`/`ModuleFlowReadStore` deliberately do not; scopes the comparison to each report's overall bucket, never a per-row breakdown |
| 0104 | An operator's name is a copy, refreshed at every sign-in, never a live join to Keycloak | Accepted — stores `Operator.DisplayName`/`Email` captured from the token's own `name`/`email` claims at invite redemption and bootstrap registration, rewritten by a conditional `UPDATE` on every `GET /api/v1/operators/me`; rejects a live join (a synchronous product→IdP dependency in the path of every screen that lists a conversation), a background Keycloak poll (same dependency, worse freshness) and leaving `operators` anonymous (blocks `23-17`/`23-18`/`23-22`); uses `IS DISTINCT FROM` so an identity with no claims does not rewrite its row for ever, and denormalises nothing, keeping `personal-data.md`'s erasure footprint at one place |
| 0105 | Capacity gates the auto-assigner only; a deliberate take charges it uncompared | Accepted — adds IOperatorCapacity.ClaimAsync beside TryClaimAsync so the difference is visible at every call site rather than hidden in a flag: the engine compares against capacity and can lose, a person taking a waiting conversation does not, because capacity prevents automatic assignment and not a deliberate act (decisions.md §2); charges it inside the handler own transaction so a losing SaveAsync cannot strand a slot; partly supersedes 0033; records the ck_conversation_assignments_source widening that 23-03 deliberately deferred until a real writer existed |
| 0106 | The buyer-facing screen reuses `site:configure` and names outcomes, not module keys | Accepted — reuses `BillingPage`’s exact gate rather than inventing an owner permission with no second caller (`authorization.md`’s own rule); keeps the product catalogue in the console because `ModuleKey` is deliberately opaque to `Ago.Chat.*` and an architecture test forbids the literal there; offers prose rather than a control for a product the tenant lacks, because provisioning is owner-only (`22-17`, `decisions.md` §6) and a button that provisions nothing is worse than words; records AGO Inbox’s channels as a real gap in the tenant-held-fact model rather than guessing at them |
| 0107 | An operator's load is a second, independently-windowed read over the same intervals | Accepted - `23-17` adds `IOperatorLoadReportReadStore` rather than a fifth method on `IOperatorAnalyticsReadStore`, because the two answer different questions over different windows: the existing store attributes a conversation to whoever replied first and windows by `conversations.created_at`, while this one asks whose assignment interval a period was and windows by `conversation_assignments.started_at`, so a conversation held twice counts once as a conversation and twice as an interval - a distinction the attribution store cannot represent, having no notion of an interval. Load buckets come from configuration (`Analytics:LoadBucketUpperBounds`) and are folded in C# rather than in either query's `GROUP BY`; no aggregate table and no read model, per `decisions.md` §2's rule that one is added when a report is measurably slow. **Written 2026-09-05 alongside the code and landed 2026-09-05 with the item's console half** - the backend merged eight hours earlier without it, which is the working-agreement miss the item's own Outcome records |
| 0108 | An archive object is rewritten, not deleted, for a conversation own erasure | Accepted - one archive object covers every conversation a site had in one retention class and month (adr/0031), so a per-conversation erasure drops that conversation own lines and re-uploads to the same key; deleting it whole would take every other conversation archived copy with it. A whole-site erasure does delete outright, but only once every conversation has already emptied it. The read reuses the presign-then-transfer shape the archive write half established rather than widening IFileStorage; the step runs immediately before the conversation row is deleted, because erasure_requested_at lives on that row and a failure after it would orphan the archived copy |
| 0109 | A person's export is scoped by visitor, and runs synchronously, not through `16-03`'s job | Accepted - `24-11` adds two granularities beside `16-03`'s whole-site export: one conversation, and every conversation one `visitors` row has, each with that visitor's contact details and channel identities. Synchronous rather than a queued `export_requests` row, because a subject-access response is small and immediate where a tenant's whole archive is neither. **Names its own limit**: it promises completeness for one `visitors` row and never that two rows are the same human - nothing in this schema makes that link, and a tenant who assumed otherwise would under-answer a real access request without knowing. Deliberately not gated on a channel identity, which answers a different question |
| 0110 | A deliberate `Away` survives a reconnect, and only the operator clears it | Accepted - `23-20` adds `Operator.GoAway()` and replaces `GoOnline` at `OnConnectedAsync` with `NoteConnected`, which raises only `Offline` to `Online`; `GoOffline` gains the same guard, because the identical defect was reachable through the disconnect path and one guard without the other looks correct and fails on the second event. Only `SetAwayAsync(false)` may overwrite a deliberate `Away` - an operator saying so, never a side effect of a connection existing. Neither hub method takes an `OperatorId`: identity is the connection's own claim, so another operator is not nameable, which is stronger than checking a permission and refusing. Rejects one `GoOnline(bool deliberate)` - it makes behaviour depend on an argument's default rather than on which named method was called. No migration: `Status` is a plain string column and `Away` was already a declared member, reachable in the schema and never written. **Written 2026-09-05 with the code and landed the same day after both had been left uncommitted in a worktree** |
| 0111 | An acceptance record survives the erasure of its own subject | Accepted - `acceptance_records` (`24-01`) is evidence that processing had a lawful basis at the time, so neither `16-02`'s conversation-scoped nor its site-scoped erasure reaches it and `subject_id` carries no foreign key to `sites`/`operators`/`visitors` - the same no-FK mechanism `adr/0101` uses for `conversation_assignments`, here for a different reason: there a workload count should outlive a visitor's erasure, here evidence should outlive any subject's. Retention is indefinite and stated rather than a byproduct of nobody having built deletion; rejects time-boxed retention on `messages.body`'s per-tier window. Held in place by `AcceptanceRecordErasureGuardTests` for both erasure scopes, so a foreign key added later for symmetry cannot reverse it silently. Accepts a real dangling reference - a `subject_id` outliving the row it named - and names the harder question it does **not** answer: if a lawyer's reading of `152-ФЗ` concludes an acceptance record must itself be erasable, this decision is superseded, not patched |
| 0112 | An erasure's own receipt names the tenant and the operator, never the person erased | Accepted - `erasure_records` (`24-13`) holds `site_id`, `requested_by`, scope, status, timestamps and per-step counts, and no `conversation_id` or `visitor_id` at all; a conversation erased under a whole-site cascade gets only the site's aggregate count, while a standalone request - the shape a person's own request takes - gets its own row. Points deliberately opposite to `adr/0111`, three items earlier: an acceptance record must name who consented, an erasure receipt that names the erased person keeps the pointer that was supposed to disappear. Reuses `0111`'s no-FK mechanism so a site's receipt survives the `DeleteSiteAsync` it proves; `failure_reason` is an exception type name, never a message, because a message can quote an object key. Rejects storing the id on `0111`'s own trade-off, a per-conversation receipt under a cascade (the least forced call here), and the deletion journal `personal-data.md` had already refused. Accepts that this table alone cannot answer *which* visitor, and that `Completed` means reachable-now, not gone from `adr/0050`'s thirty-day backups |
| 0113 | An access record names the resource read — the mirror image of an erasure receipt naming none | Accepted - `access_records` (`24-12`) carries `resource_kind` plus a bare `resource_id` whenever the access named one, the **opposite** of `adr/0112`'s choice one item earlier and deliberately so: an erasure receipt exists to prove data is gone, so naming the person keeps the pointer that was meant to disappear; an access record exists to prove who read what, so withholding the resource answers a question nobody asked. Records `18-07`'s cross-conversation read and the platform owner's five cross-tenant surfaces; refused attempts are not recorded, because a caller who read nothing has nothing to attest to. No foreign key on `site_id` or `resource_id` - a third use of `0111`/`0112`'s mechanism, here so a record of an access survives the tenant's own erasure, or the tenant most likely to ask loses the answer to the very process they are asking about. Retention is **365 days with a prune job**, diverging from its two indefinite siblings because this is evidence about the operator, not about the visitor. Names its own gap: `customer:read` in AGO Calendar is a different repository and a different database, and is not covered |
| 0114 | A document's text is data; only publishing and reading it is code | Accepted - `24-02` stores documents and their versions in Postgres and exposes publishing behind `RequirePlatformOwner`, so a wording fix, a new version or a whole new document is one authenticated call rather than a commit, a PR and a deploy. That is what makes the author's own sequencing work - **build the mechanism now, have a lawyer validate the text afterwards** - because a lawyer's verdict changes a row. No legal text is committed to this public repository. Version identifiers are server-derived `v{n}` from the aggregate's own counter: stable, ordered and human-quotable at once, which no caller-supplied string gives. Neither `Document` nor `PublishedDocumentVersion` has a rename, update or edit method at all, so a correction is structurally a new version. Rejects text-as-file-in-repo, a single overwritten current row, caller-chosen versions and content-hash versions. Leaves open, deliberately, whether a new version invalidates an existing acceptance |
| 0116 | A channel delivery names the channel identity it went through, never the address | Accepted - `channel_deliveries` (`23-19`) references `channel_identities.id` with a real enforced foreign key and never stores `ExternalChannelAddress`, because a delivery row is written once per outbound message and storing the address would grow a second copy of the value `channel_identities` already minimises to one row per site and address. **Deliberately does not copy `adr/0112`/`0113`'s FK-less shape**: those give up referential integrity to survive an erasure of their subject that happens while the table lives on, and no such case exists here - `ChannelIdentity` is never hard-deleted on its own (`Unlink` only sets `Active = false`, checked rather than assumed) and its only hard delete is the site erasure that removes this table's rows in the same statement. `conversation_id` still carries no key, so the record outlives a single conversation's own erasure. Names the condition that would reopen it: the day a `ChannelIdentity` becomes hard-deletable independently of its site, this foreign key stops being safe |
| 0118 | Revoking a tenant's own purchase is refused unless forced, and the override is recorded | Accepted - `RevokeModuleForSiteAsOwnerHandler` (`23-13`) reads `GrantedByOwner`: a self-service purchase is refused unless the caller states `Force` **and** a non-blank reason, while an owner-made grant revokes exactly as before. The asymmetry is the decision - revoking what we gave away must not get harder, revoking what somebody paid for must. Every exercised override lands in `module_revoke_overrides` with **free-text** reason, deliberately unlike `erasure_records`' exception-type-name discipline, because a justification that cannot name what happened is not one. No foreign key on `site_id` - the fourth use of `0111`/`0112`/`0113`'s mechanism, here because the tenant whose purchase was overridden and who then closes the account is the one most likely to ask who did it and why. Rejects folding this into `access_records` (which holds *that* a boundary was crossed, never *why*) and the `required`-nullable trick `ExpiresAt` uses (omitting `Force` is never ambiguous). Records that **`module_grant_audit` is not a table** - the migration of that name added two columns to `enabled_modules`, and a reader who goes looking will otherwise hunt for it |
| 0123 | The contact-visibility rung is one setting on the account, carried by its own minimal event — and rung three does not exist in the type system | Accepted - `23-11` puts a two-value `ContactVisibility` on `sites` and masks chat's own `visitor_contact_details` in the read model. **`Never` is absent from the enum entirely**, not gated behind a flag: `decisions.md` §5 forbids selling it until the system can place the call itself, and a value present in code is a value a screen or a sales conversation can point at. One domain write maps to **two** integration events - the existing `SiteSettingsChanged`, so the existing cache-invalidation consumer works unchanged, and a new `ContactVisibilityChanged` for `23-12`'s calendar-side consumer, because folding a cross-product fact into a contract that has never crossed the boundary would blur one this project has kept clean since `RoleAssignmentsChanged`. A reveal writes its own `contact_reveals` table rather than widening `adr/0113`'s `AccessRecordKind`: the revealing operator crosses no boundary they did not already hold `ConversationRead` over, so it is not that enum's shape, and §5 wants reveal counts in an audit view rather than diluting a cross-tenant log. Reveal is **not** gated on the rung - the rung governs the list, not who may reveal. Accepts a shape new to this codebase (one domain event, two integration events) and a second small prune job beside `access_records`'. **Carries the calendar side too, folded in 2026-09-06 (`23-12`) rather than split into its own ADR at the author's call** - the ladder is one decision about one account setting, and a reader who opened either half would have seen half of it: the calendar gets its own `contact_phone_reveals` (there is no `access_records` here to widen, so the question collapses to building one), confirming a number by phone is gated on `CustomerRead` alone because no rung that exists hides a number from a `CustomerRead` holder, and the projection commits in one save because a rung has no prior value to decide from. A missing projection row reads as `Visible`, so an undelivered message never becomes a policy |
| 0124 | Blocking a conversation is one state, and a blocked visitor's inbound message is accepted, stored, and left unprocessed | Accepted - `24-10` adds `conversations.blocked_at`/`blocked_by`, reversible, with each block and each reversal recorded as a **separate act** in `conversation_block_records`. **One state, not two**: nothing named a need for a "hidden but still processed" rung, and a second one would double every read-path assertion the item's own Done-when requires enumerating individually. Cannot reuse `erasure_requested_at` - erasure is terminal and short-windowed, blocking holds indefinitely, is reversible, and must coexist with an erasure requested on the same row. The write bypasses the aggregate (raw SQL), the identical cost `erasure_requested_at` already rejected loading a full message history to flip two columns for. **An inbound message from a blocked visitor is still accepted and stored**: refusing silently loses a real customer's message with no trace, and refusing visibly would make AGO's widget state something about a visitor's data status that `adr/0076` assigns to the tenant. `conversation_block_records` carries a real FK with cascade - the first place in this stage that deliberately does **not** reuse `adr/0111`'s no-FK mechanism, because a block describes current reversible state rather than evidence that must outlive its subject. Names two residuals: write paths beyond auto-reply and auto-assignment are not frozen, and `blocked_at IS NULL` is a predicate repeated by hand across about a dozen queries with nothing enforcing that a future read remembers it |
| 0125 | The calendar add-on's quota crosses products through the outbox; the calendar owns enforcement and the downgrade rule | Accepted - `22-07`. Rule 8 forbids a write decision reading a cache, and a synchronous cross-product ask **is** a cache by the time the calendar's own transaction commits, so chat publishes an opaque fact (`ModuleQuantityGranted`: site, module key, quantity) that never names "calendar", and the calendar enforces against its own row. Enforcement is `SELECT ... FOR UPDATE` plus a real `COUNT(*)`, the idiom `OperatorInviteRedemptionRepository` already uses for seat limits, not a denormalised counter - worker creation is rare and not a hot path. **A downgrade deactivates rather than refuses, and deletes nothing**, choosing the most recently created active workers first: the property that makes it defensible is predictability, not fairness, since a tenant sorting by "added on" sees exactly what a downgrade would cut. The consumer commits in two saves rather than one, a deliberate deviation from `RoleAssignmentsChangedConsumer`, safe only because a grant is a snapshot and re-applying an identical value is a no-op. Its own kindness is what makes `22-23` possible - the rows survive, so an ungated reactivation walks straight around the quota |
| 0126 | *(never written - the number was drafted for `23-12`'s calendar-side decisions and then folded into `adr/0123` at the author's call, so the ladder lives in one file. Kept vacant rather than reused: a reference to `0126` anywhere is a pointer at that draft, not at a different decision)* | n/a |
| 0128 | A visitor-supplied contact detail is the same row with its provenance stated, and `Verified` is a fact nobody can assert about themselves | Accepted - `23-09`. The visitor's own number goes in `visitor_contact_details` beside the operator-typed one, and deliberately **not** in `ChannelIdentity`: that table means traffic arrived or a code was confirmed, and treating a typed number as one would make this system's strongest claim about an address out of its weakest evidence. `source` is a real column rather than inferred from a null `recorded_by_operator_id` - the same refusal `VisitorContactDetail` already makes for `Kind` - so the null becomes a *consequence* of `Source = Visitor` rather than the fact itself. **`verified` starts false and no path a visitor can construct sets it**, because a flag a subject asserts about themselves is worse than no flag: a reader trusts it. The migration backfills `source` to `Operator` by a **hand edit over EF's generated empty string** - the one place here where regenerating mechanically produces a silently wrong result, a value no enum member parses back from. Supersedes `personal-data.md`'s claim that a row here never comes from the visitor, which was true of `14-14`. Names its own cost: `verified` has one writer that always writes `false` until a verification path exists |
| 0127 | A visitor's consent gates every write of a contact detail, on both entry points, and the tenant publishes their own consent text | Accepted - `24-05`. A per-site switch, **off for every existing site**, and where it is on the gate attaches to **handing over a phone or email, never to the conversation** - proven by `ConsentGateDoesNotBlockConversationTests`, which exists for nothing else. Gates **both** write paths, the visitor's own form (`23-09`) and an operator promoting a number from the transcript (`23-10`), because a visitor-typed number is the same class of data whichever of them structured it. **No new store**: `acceptance_records` already carried `Visitor` as a subject kind and this is its first real writer and reader; the tenant's own text rides `24-02`'s document mechanism through a second, permission-gated entry point rather than a parallel store. Any past acceptance satisfies the gate, deliberately, because `adr/0114` left "does a new version invalidate an old acceptance" open and a version-strict check would silently re-demand consent. Checked against the statute **after** it was written: it satisfies the 1 September 2025 rules on pre-ticked boxes and on not combining purposes, and **does not answer** whether a tick against a versioned document is *«отдельный документ»* - filed as `25-02`'s D7 rather than assumed |
| 0129 | A muted navigation entry means "you can obtain this yourself" - replacing "a colleague here could grant it" | Accepted - `23-31`. Supersedes `23-24`'s rule (`decisions.md` §10). Muting used to say *a colleague at this tenant could grant this*, which was true of the calendar and never true of the three permission gates it was generalised to - nobody buys `site:erase`. The replacement asks whether **this identity** could obtain the thing itself, which is true only for a `site:configure` holder and only for the calendar module. Consequence, taken knowingly and named in `23-24`'s own terms: **an operator now sees nothing muted at all**, so they no longer learn a feature exists and cannot think to ask for it - accepted because no operator action ever hung on that knowledge and their console gets dramatically shorter. The lock glyph is **deleted rather than repointed** (a padlock reads as *restricted*, not *for sale* - reusing it would be a visual pun worse than nothing), which closes `adr/0030`'s one-glyph exception and re-opens `gaps.md` pile 3 item 3 |
| 0133 | Removing a team message is a tombstone, gated on `site:manage_operators`, with its own cascading record | Accepted - `23-33`. **Not a delete**: `23-32`'s room is append-only with a server-assigned sequence and the reconnect catch-up pages by keyset over it, so a removed row would punch a hole the delta path is exactly where it shows. The body is **not scrubbed at rest** - the read store nulls it in projection, so removed text never leaves Infrastructure, but the row keeps it; nothing in scope asked for minimisation and `personal-data.md` retains this table with no tiering machinery to strip one column safely. The gate is `site:manage_operators` checked **live**, never the message's stored `AuthorIsAdmin` label, which answers *was the author an admin then* rather than *does the remover hold the power now* - the same permission `23-32` asks for a label, asked here for an outcome. **An operator cannot remove even their own message**: self-deletion is a different power with a different record, and rule 15 makes it a second ticket. The accountability row **cascades** from `sites` and from the message, diverging from `module_revoke_overrides`'s deliberate no-FK - that record must outlive an account closure, this one's subject is a room `23-32` already erases wholesale, so surviving would make it an orphaned fragment of personal data |
| 0137 | TLS between services, scoped to the chat -> calendar module hop, on a bring-your-own internal CA | Accepted - `22-24`, the author's reading B. A second Kestrel endpoint on `ago-calendar-api`, configured entirely through environment because `Program.cs` never overrides Kestrel's config-driven binding (checked, not assumed), plus a namespaced cert-manager `Issuer` signing one leaf. **The root is generated offline rather than by cert-manager's own in-cluster self-signed pattern, and the reason is ordering**: the trusting side is a container image CI builds before any cluster exists, so a root the cluster generates at apply time cannot be baked into an image that predates it. The certificate is committed in both repositories (a certificate is not a secret - `secrets.md`'s own reasoning for a site's public key); the private key never is, and the one shipped is a placeholder whose key was destroyed after proving the mechanism. **Scope is one hop and that is a decision**: Postgres, Redis, RabbitMQ, MinIO and Keycloak stay plain, because doing one hop while implying all of them is how a compliance line gets ticked falsely. The leaf's expiry needs no new alert (`TlsCertificateRenewalOverdue` already has no per-certificate matcher, proven with a real `promtool` run that also found a hardcoded certificate name in that rule's own annotation); **the root's expiry has no automated alert at all**, named as a gap because pointing a cert-manager `Certificate` at that Secret would make it reissue over an offline root. Demonstrated with Docker and `openssl` alone: a real image build trusts a leaf this CA signs under default validation, and the same build without the one added step rejects it with `PartialChain` |
| 0136 | The queue is computed, not kept - readiness and Done-when settlement are derived from the open issues on every ask | Accepted - `23-47`, `23-50`. Both items first offered two readings and **both readings were wrong in the same way**: they argued about *where* an ordered list lives, when the staleness each was avoiding disappears if there is no list. So `queue-audit.sh --ready` computes which open items are startable, fresh on every run, from `Status` and `Depends on`; `--partial` lists items with a partial Done-when, so the periodic sweep is a command rather than a favour; and the default run gains the opposite of its existing staleness check - **a closed issue with an unticked box**, which is the shape all three of 2026-09-06 real defects took and which nothing caught. Readiness is a flag on that script rather than a sibling because it needs the same issues-and-files data the audit already reads, unlike `secrets-audit.sh`, whose separateness comes from genuinely different sources. **It reports what it cannot decide instead of guessing**: a qualified `Status`, an OR-shaped dependency, or one naming no item number is surfaced for a person, never silently called ready |
| 0140 | The widget's cached configuration is refreshed on its own daily clock, not alongside the identity token's renewal window | Accepted - `23-54`. A returning visitor's widget made **no network request at all** unless the stored identity token had entered its own renewal window - the last third of a seven-day life - so a colour or position changed in the console stayed invisible for up to four days and sixteen hours, and `Ctrl+F5` could not help because the staleness was in `localStorage`, not the HTTP cache. Config freshness and identity renewal are now **two independent conditions feeding the same `renew()` call**, because they answer different questions - *is this rendering preference current* against *is this identity worth keeping alive* - and conflating them is exactly what produced the defect. Twenty-four hours, reading the token's own `nbf`/`iat` rather than storing a parallel timestamp, so sessions already on a visitor's device get the new behaviour with no response-shape change. Refetching on every page load was rejected as a per-visit round trip for every tenant's site, for a preference that changes rarely. The console's own copy said changes take effect *the next time a visitor's page loads the widget*, which was false for every returning visitor - that mismatch is what made this a defect rather than `adr/0029`'s already-accepted open-tab gap |
| 0141 | A visitor token that can write a conversation can read it | Accepted - `23-53`. Reopening the widget showed an empty transcript because `ago-widget` sent a fresh page load's *stored* `lastKnownSequence` cursor to the same join call a live reconnect correctly uses it for - a present, already-caught-up cursor gets an empty delta, which is right for a connection whose DOM already holds everything up to it and wrong for a DOM that has rendered nothing yet. The server-side read this depends on needed no change and was already correctly scoped by the signed token's own `VisitorId` claim (`NoteLeakProofTests`, `18-04`, plus this item's own new tests: an operator note and a real system event - an assignment - in the same conversation, neither reaches the visitor). What the fix does add is real: a shared device now *reads* a conversation it could previously only *continue* - **accepted rather than bounded to the current session**, because the token already had that reach on the write side (`SendMessageAsync` checks nothing beyond it) and a read bound the write path does not honour would only delay the same exposure by one message-and-reply exchange, not close it |
| 0142 | A service's price is honest, not exact - kopecks, an optional "от" floor, and never repeated in the booking confirmation | Accepted - `23-35`. The author answered that a service has a price and a description; three things were left to the change. **`Money` is minor units plus a currency code mapped as one nullable EF complex property**, not two independently settable columns - the same "two columns that must agree are two columns that can disagree" reasoning `data-model.md` gives `sites.demo_expires_at`/`is_demo`, so "an amount with no currency" cannot be constructed. An integer rather than a `decimal`, for the reason `Service.Duration` already is one. v1 accepts **RUB only**, validated in the constructor rather than by a `CHECK` (the `widget_notice_url` precedent for text that is not a closed set); zero is a real price, `null` is the absence of one. **`PriceIsFrom` refuses to pick a universal reading of «от 2000 ₽»**: the tenant says per service whether the number is exact or a floor, normalised to `false` when there is no price so the two cannot disagree. Shown on every booking-capable surface from one `BookableServiceRow` (`20-06`'s constraint) and **never carried into `BookingConfirmedResponse`** - a number offered beforehand is a courtesy, the same number in a confirmation reads as a commitment this product does not enforce. The description takes `adr/0114`'s reasoning (a wording fix must not need a deploy) without its mechanism: a documents-and-versions system for a one-paragraph blurb is generalisation ahead of need |
| 0143 | A channel status read asks the provider live - the second real caller of `Decrypt` outside an outbound send | Accepted - `23-36`. "Connected" must mean the provider agreed, not that a string was saved, so `GET /channels/telegram` re-asks Telegram's own `getMe` on every read rather than trusting the stored `Active` flag - which only ever proved the token was accepted once, at registration. This corrects a sentence in `adr/0069` that was **already narrower than reality before this item** (both long-polling services decrypt to poll, which is inbound, not outbound send); `adr/0069` keeps its text and this one says where it was wrong. **The live call is bounded at five seconds**, matched to `ConfigureChannelResilienceDefaults` rather than invented, through a token linked to the caller's own so an ordinary cancellation still propagates as cancellation - unbounded it inherited `HttpClient`'s 100-second default, tolerable on a connect a person just pressed and not on a read that happens on every page load through a SOCKS proxy that can be down. The same pipeline's retry and circuit breaker are deliberately **not** reused: retrying a page-load read makes an unreachable provider's page slower, not more honest. "Could not reach Telegram" and "Telegram says this token is not valid" stay structurally separate to the screen, because a tenant acts differently on each. **The gap stays named**: a revoked bot still produces no unprompted notification, only an honest answer the moment somebody looks |
| 0144 | `redeploy.sh` warns on manifest drift it cannot apply; it does not apply manifests itself | Accepted - `15-21`. `redeploy.sh` and `deploy.sh` move the cluster with `kubectl set image` alone and never apply the overlay - which is how `23-45`'s configuration was correct in the repository, absent from the cluster, and crash-looped the API on the next redeploy, with nothing down only because the old ReplicaSet kept serving. `check-manifest-drift.sh` runs last in `redeploy.sh`, non-fatally: renders the overlay, keeps **only Deployments and NetworkPolicies**, neutralises every image tag against what is actually running so it never fires on the routine gap between an image move and committing the tag, and reports PASS, DRIFT with the diff and the order out of it, or **UNKNOWN - never folded into a false PASS**. Warn rather than apply, because folding an apply into an image roll pushes whatever else drifted into the overlay since the last deliberate apply; warn rather than refuse, because a refusal fails a deploy that already succeeded. Rejected: inverting the ordering to apply-then-image (re-opens `8-08`/`8-12`'s migrator ordering for no incident that ordering caused) and a hand-rolled structural diff (reinvents `kubectl diff`'s three-way-merge defence against server-defaulted fields, badly). **Unproven against a real cluster**: `15-21` merged before the 2026-09-07 deploy and did not run in it, because `git pull` replaces the script file and the running bash finishes the version it began with |
| 0146 | A tenant's own "who accepted" screen omits the client IP and user agent the record also holds | Accepted - `23-37`. `24-01` kept `ClientIp`/`UserAgent` on an acceptance record because a consent record is commonly indefensible without them; `23-37` is the **first screen that reads that table back**, and it could trivially have handed both to a tenant. Whether it should is a real question, so it is decided rather than answered implicitly by whatever response shape somebody happened to write. `GetSiteConsentAcceptancesHandler`'s DTO carries `SubjectKind`, `SubjectId`, `DocumentVersion` and `AcceptedAt` **only** - the omission is structural, not a rendering choice downstream. The columns stay in Postgres and stay reachable to a platform owner investigating an incident: **this narrows one read surface, not the record**. `personal-data.md`'s existing stance already treats an IP as more directly identifying than a bare opaque id, and `acceptance_records` carries no foreign key to any subject (`adr/0111`) and is retained indefinitely as evidence of a lawful basis - which is what makes "the data must exist" and "the data must appear on an ordinary settings screen" pullable apart. Named cost: a tenant who needs the fuller evidentiary shape - the reason `24-01` kept those columns at all - cannot get it here |
| 0147 | A contact collected in chat reaches the calendar without either product reading the other | Accepted - `23-58`/`23-59`/`23-60`. `adr/0093` keeps two schemas in two databases, so **chat publishes through the outbox in the same transaction as the contact write (rule 4), and the calendar consumes when the module is granted**, idempotently (rule 5). Granting the module **carries over every contact ever collected**, as a replayable pass rather than a side effect of the grant - so a grant that succeeds and a carry-over that fails stay two facts. A phone matching an existing customer **does not merge**: the asymmetry decides it, since a duplicate is visible, reversible and embarrassing while a wrong merge is invisible, irreversible and shows one person another person's bookings (`23-60` gives the manual merge that makes the refusal honest). Rejected: the calendar reading chat's tables (one product's schema in the other's queries, and the calendar cannot start when chat is down); chat calling the calendar's API from a request handler (a visitor's form submission would depend on another product being up). Named costs: duplicate customers become normal, the backfill is unbounded, and **deleting a contact in chat does not delete the customer in the calendar**. The consent wording `24-05` carries has to actually permit the retroactive carry-over - a question for the lawyer reviewing `23-52`, not for this ADR |
| 0148 | A greeting is drawn, not sent, and nothing exists until the visitor writes | Accepted - `23-64`. An auto-opened widget shows a first line from the shop's side that **is not a message**: no author, no row, no event, nothing server-side, and deliberately kept out of the rendered history's own data path - `23-53` is why that last part is written down rather than assumed, since a client-only entry in the list the server populates is exactly how an empty or duplicated transcript gets produced. The constraint is the author's and it is the whole design: a conversation that exists because a timer fired is a queue row, an unread badge, a line in `23-18`'s analytics and an assignment, **all describing something that never happened**. Rejected: creating the conversation and sending a real greeting (what most products do, and it fills the queue with rooms nobody entered); persisting a pending message and promoting it if answered (the closest rival, kept open for `23-64`'s own question about materialising **at the moment the visitor writes**). Named costs: the transcript may not explain itself to the operator, a drawn greeting is in no record a visitor could later ask to see, and the widget now renders something that is not a message in a list that otherwise contains only messages |
| 0150 | The console acts as platform owner, and the browser never holds the provisioning secret | Accepted - `23-65`/`23-66`, amending `adr/0095`'s holder rather than its purpose. `decisions.md` §6 had already chosen this as its own later half - runbook now, chat holding the secret in its configuration later - and the author asked for that half on 2026-09-07. The count is the argument: **`enabled_modules` held zero rows against seventeen sites**, because the only way to give somebody a product was a hand-made request carrying a deployment-wide secret. §6's own reasoning stands - the platform owner can read that secret from the cluster anyway, so requiring them to paste it is not a second lock but a second inconvenience. **What this ADR adds is the half §6 did not say**: the secret stops being something a person holds for a moment and becomes something a service holds permanently, widening what a compromise of `Ago.Chat.Api` yields - accepted because that process already holds the channel-credential cipher key and the database credentials. `expiresAt` stays required and a screen must make "never" a selection rather than a default; `adr/0118`'s force-and-a-reason asymmetry for revoking a purchase is unchanged; every grant and revoke is recorded. Rejected: keeping the runbook (a product nobody can be given is not a product), prompting for the secret in the browser (§6: a secret carried in a clipboard stops being one at about the third use), and a separate owner-only service (a deployable to operate for one capability) |
| 0149 | A tenant's lifecycle crosses to a module by a lease and by a proof, never by a fire-and-forget event | Accepted - `22-08`/`22-30`/`22-31`/`22-32`. A module keeps serving on a lease it renews; chat's answer to that renewal is the whole mechanism, so a broker outage cannot silently leave a suspended tenant served. Both parameters were the author's and both were answered 2026-09-07: suspension is a commercial lever, and L is 24 hours renewed at 12. No number here was measured - the argument is from the asymmetry of costs, which is the only ground available |
| 0151 | Entitlement is granted by the platform; configuration inside it is the tenant's | Accepted - `23-83`/`23-84`/`23-85`/`23-86`. A permission is not an entitlement: `channel:manage` answers whether this person may act for the account and cannot answer whether the account has bought it. Every capability in the price list gets an entitlement, checked in the handler rather than the screen; the tenant's own credential stays configuration and remains theirs |
| 0152 | The sign-in-and-manage invariant is already closed; releasing a seat needs no guard of its own | Accepted - `23-67`. `23-26` refuses removing a site's last `site:manage_operators` holder under a row lock, and `23-71` makes that permission alone sufficient to sign in, so `ToggleOperatorSeatHandler` - which writes `HoldsSeat` and never touches a role - cannot move the fact the invariant is about. Also answers the author's own question: 'cannot remove the last one' and 'cannot remove yourself' do not contradict, but the second must not be built, because its only distinct effect is refusing a safe self-removal when a colleague administrator remains |
| 0153 | A key-shaped console field generates its own value in the browser | Accepted - `23-94`. The grant form's Credential field had nothing producing a value, and `ModuleCredential`'s length floor cannot tell a random key from a typed one, so a person without a terminal types something and it passes. `crypto.getRandomValues`, 32 bytes, base64 - the shape `openssl rand -base64 32` already produced. Client-side despite this codebase's server-side convention for minting secrets, because every one of those mints a value the server itself must own or verify and this one is accepted as opaque input either way; a route would only push the secret across the network before the owner has decided to submit it |
| 0154 | A module's entry point is configuration, not a caller-supplied field | Accepted - `23-92`, applying `adr/0150`'s reasoning to the field beside the one it moved. `EntryPoint` was free text because the model makes it a per-(site, module) coordinate - the shape wanted if one tenant's calendar lived on a different deployment, which AGO has no case for. `IModuleEntryPointProvider` resolves whatever key a caller names against what the deployment declares, so no literal module name enters `Ago.Chat.*` and the architecture guard still holds. An undeclared key is refused by name rather than left blank to fail later as a 404 |
| 0155 | A tenant erasure reaches a module over the provisioning channel, and a module revoke tombstones rather than deletes | Accepted - `22-30`, qualifying `adr/0149` rule 2's own 'credentialed per-site channel' phrase. That channel is exactly what a revoked tenant lacks, because revoke stops the per-site credential authenticating by design (`22-11`) - so erasure uses the deployment-wide provisioning secret, on its own route, since folding it into revoke's would give one HTTP verb two irreversible meanings. Revoke now stamps `RevokedAt`, because the row it used to delete is chat's only record that a site ever had the module at all. Named cost: a revoke-then-re-enable cycle leaves two rows, and no uniqueness constraint on (SiteId, ModuleKey) existed before this either |
| 0156 | A current-state document is authoritative for what holds; an ADR says why | Accepted - 129 ADRs, 178,000 words, and both "how is this done now" and "how did we get here" were being answered by reading the chain and reconstructing. The first question is asked every session and the second rarely, so the expensive path was being spent on the cheap question - and unreliably, since exactly **one** ADR carries a machine-readable supersession link while 25 discuss it in prose, and amendments were being appended inside accepted ADRs, so a file can look current and contain its own reversal. `docs/architecture/*` now answers what holds; the ADR answers why. Named cost: a document that silently omits a decision gives a confident wrong answer, which is why `queue-audit.sh` checks every ADR has a home rather than leaving it a convention |
| 0157 | Forward is told from rollback by asking the cluster what it has already run | Accepted - `23-110`. `22-24`'s guard refuses a manifest naming a tag nothing is running, which is a rollback *and* an ordinary forward deploy from freshly published CI images - identical to a set difference. So the only escape hatch, `--force-rollback`, was also the only way to move forward, and a deploy of three repositories on 2026-09-08 had to be spelled with it. Now a tag found in ReplicaSet history is a rollback and needs the flag; a tag never seen is forward and needs nothing, so the flag is truthfully named and no second one is required. Rejected: a `--roll-forward` twin (leaves the operator to classify the deploy at exactly the moment they are wrong about it) and a git descendant test (needs the node's checkouts fetched, and they have been 85 commits stale). Named hole: a rollback older than `revisionHistoryLimit` reads as forward - which is deliberate archaeology, never the accident this guards against |
| 0158 | The registry is asked before building, and a Job that cannot start is not a running migration | Accepted - `23-109`. `23-98` recorded that the evicted migrator images were "unrecoverable without another build"; that was an inference and it is false - both CI workflows push their migrator, the packages are public, `imagePullPolicy: Never` is gone since `15-06`, and GHCR holds 101 tags of `ago-chat-migrator` resolvable anonymously, so the kubelet re-pulls an evicted one unaided. The real exposure is only what this node builds and nothing else has, so the build now pulls what the registry holds and builds only what it lacks - **per image**, because five are built while four are published and skipping a repository wholesale would have stopped building `roleassignmentbackfill` entirely. `registry_has` answers present / absent / could-not-ask, and could-not-ask means build. Separately: a Job whose container reports `ImagePullBackOff` is not a running migration, so it stops blocking the next apply - the distinction is cannot-start versus working, never slow versus quick, which is why a timeout would be wrong |
| 0159 | An option is its own subscription, aligned to the account's base period | Accepted - `23-86`. `adr/0151` says an entitlement comes from the platform owner *or from the system on a payment*, and the second half did not exist - not as a missing join but as a missing noun: `BillingSubscription` carries `Tier` and `RequestedSeats` and nothing else, so a purchased option could not be written down. Each option is now its own subscription whose `CurrentPeriodEnd` is copied from the base, so both renew on one date and the annual discount applies to the set. Chosen over lines in one subscription because the commercial grid requires that a failed option charge not endanger the tier, and a single charge either succeeds or fails - a declined 100 rouble channel would have put a paid tier into `PastDue`. Free consequence: `ListDueForRenewalAsync` is set-based, so options renew and lapse through the existing job untouched. Named hazard: `GetBillingStatusHandler` means "the site's subscription" and would silently return whichever row is newest |
| 0160 | A paid option outlives the tier that lapsed under it | Accepted - `22-33`. `adr/0073` chose downgrade-not-stop for the tier and nobody had extended the reasoning to a paid add-on. The author's answer: an option runs on its own subscription and its own money, so a base lapse does not cancel it - the account falls to the free tier and the option keeps working while it is paid for, "maximum a year" being the horizon of one paid period rather than a stop. Implemented by **declining to add a mechanism**: `adr/0159` already leaves the two rows with no relationship a charge can traverse. Rejected: lapsing the option with the base, whose premise is false since the option *is* still being paid for. Found while checking it against what was already decided: the auto-deletion exemption is written under the paid tier's heading, so a free-tier account that is paying would be deleted for inactivity - `23-116` |
| 0161 | A customer merge tombstones the losing row and is never undone | Accepted - `23-60`, the other half of `adr/0147`'s own refusal to auto-merge. A merge is permanent (`Customer.MarkMergedInto` throws on a second call) and the losing row is tombstoned, never deleted, since `Event.CustomerId` carries a real foreign key and history is kept forever. The system chooses the survivor rather than the operator - found while building the write path: `BookingStore`'s own upsert target can only ever be satisfied by a Booking-sourced row, so an operator picking a Chat-sourced survivor would silently misroute that person's next booking onto a third row. `customer_merges` carries real foreign keys and cascades with the tenant, unlike `contact_phone_reveals` - a merge is the tenant's own act on its own data, not evidence AGO must answer for. Named cost: no undo, ever, stated as a real cost rather than a footnote |
| 0162 | The saved-conversation archive is a hand-rolled ZIP writer, not a library | Accepted - `23-62`. The visitor-facing "save conversation" file is a `.zip` with an HTML transcript and real attachment bytes, decided in the item itself. The browser's `CompressionStream("deflate-raw")` gives DEFLATE - the ZIP format's own compression method 8 - for free, no polyfill; what it does not give is the ZIP container (local file header, central directory, end-of-central-directory record) or the CRC-32 checksum, so `zip.ts` writes those by hand (~200 lines) rather than adding a dependency. Rejected the pragmatic default (`jszip`/`fflate`) on the numbers, not on principle: either would ship its own DEFLATE implementation, duplicating what the platform already gives free, at real cost against `ago-widget`'s hard, CI-enforced gzip ceiling - the reason this item's own backlog asked the question before implementation began. Falls back to ZIP method 0 (store, uncompressed) if `CompressionStream` is unavailable - the archive stays valid, only bigger, never absent |
| 0163 | A tenant can relax the verified-phone guarantee, until a gateway exists | Accepted - `25-39`. `14-15` shipped with no live SMS/voice gateway account and no date on that decision, so `20-09`'s `RequiresVerifiedPhone: true` was making every chat-driven booking uncompletable, not protecting anything. A per-site, off-by-default `WidgetConfig.AcceptUnverifiedPhone` lets `BookEvent.RequiresVerifiedPhone` become `!acceptUnverifiedPhone`, computed fresh on every reply - with a known phone already in hand, the booking completes with no phone step shown at all; with none known, the step still shows but demands no proof. `PhoneVerifiedAt` stays `null` on this path, never fabricated. Rejected: waiting on the vendor decision indefinitely; silently treating an unverified number as verified (corrupts the signal for good); a global rather than per-tenant flag. `20-10`'s public booking widget is deliberately not covered - it already has a working verification mechanism of its own |
